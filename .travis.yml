language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t jayunewal/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jayunewal/docker-react npm run test